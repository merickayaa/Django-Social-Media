# Üretmek için: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=buraya-yeni-bir-django-secret-key-uret

# Sitenin çalıştığı alan adı, lokalde localhost
APP_HOST=localhost

# MySQL veritabanı kullanıcı bilgileri
DB_USER=
DB_PASSWORD=